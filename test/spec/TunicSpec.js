'use strict';

var Tunic = require('../../lib/tunic');
var tunic = Tunic;

describe('Tunic', function () {
    it('should', function () {
        expect(Tunic).toBe(tunic);
    });

    describe('#_transform', function () {
        it('should', function () {
            expect(Tunic).toBe(tunic);
        });
    });
});
